"use script"

window.addEventListener('load', () => {
    alert("Bienvenido to DashBoard!");

    const work_hours = document.querySelector(".work-hours");
    const input_value = input.value;
   
    input_value = "0Hrs";
});